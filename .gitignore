# Local-only working folders. Not part of the site, never committed.

# Untouched original media, kept as a backup after re-encoding.
_originals/

# Staging folder used during the media migration.
_r2-upload/

# Scratch: files the Cowork device bridge could not delete itself.
_to_delete/
"digital/data.local-only/" 
